- binary_sensor:
  #####################################################
  # Weather                                           #
  #####################################################
  - name: 'Dark Outside'
    unique_id: ce7cb6b7-c666-494b-aa3c-4ed79b2f1092
    state: >
      {% if ( 
        states('sun.sun') | is_defined and 
        states('sun.sun.attributes.elevation') | is_defined and
        states.sun.sun.attributes.elevation | is_number and
        states('weather.home') | is_defined ) %}
          {% set elev = 'states.sun.sun.attributes.elevation' | int(0) %}
          {% if ( elev < 6 ) %}
            true
          {% elif ( elev < 10 and is_state('weather.home', 'partlycloudy' ) ) %}
            true
          {% elif ( elev < 15 and not is_state('weather.home', 'sunny' ) ) %}
            true
          {% else %}
            false
          {% endif %}
      {% else %}
        true
      {% endif %}
      {# This is using the MET integration for weather. Available conditions: #}
      {# https://github.com/home-assistant/core/blob/70648d6e3b42bb0910e33e5c6a4e1e8c0789d82c/homeassistant/components/met/const.py #}
    availability: >
      {% set items = ['sun.sun','weather.home'] %}
      {{ expand(items)|rejectattr('state','in',['unknown','unavailable'])
        |list|count == items|count }}
    icon: mdi:snowflake-alert
    attributes:
      friendly_name: "Dark Outside"

  - name: "Washing Machine"
    unique_id: 0f504a93-327b-4a03-ab93-4fc6aaaecbae
    device_class: power
    delay_off:
      minutes: 7
    state: >
      {% if (is_number('sensor.wash_machine_energy_current')) %}
        {% if (states('sensor.wash_machine_energy_current') | float( 0.06 ) > 0.085) %}
          true
        {% else %}
          false
        {% endif %}
      {% else %}
        false
      {% endif %}
    availability: >
      {% set items = ['sensor.wash_machine_energy_current'] %}
      {{ expand(items)|rejectattr('state','in',['unknown','unavailable'])
        |list|count == items|count }}
    icon: mdi:power
    attributes:
      friendly_name: "Washing Machine"

  - name: 'AC Enabled'
    unique_id: f885f9fd-b280-439b-81f0-3fdd713393f5
    state: >
      {% if (
        states('weather.home') | is_defined ) and
        states('weather.home.attributes.temperature') | is_defined %}
          {% set temp = 'weather.home.attributes.temperature' | int( 41 ) %}
          {% if ( temp > 40 ) %}
            true
          {% else %}
            false
          {% endif %}
      {% else %}
        true
      {% endif %}
      {# This is using the MET integration for weather. Available conditions #}
      {# https://github.com/home-assistant/core/blob/70648d6e3b42bb0910e33e5c6a4e1e8c0789d82c/homeassistant/components/met/const.py #}
    availability: >
      {% set items = ['weather.home'] %}
      {{ expand(items)|rejectattr('state','in',['unknown','unavailable'])
        |list|count == items|count }}
    icon: mdi:snowflake-alert
    attributes:
      friendly_name: "AC Enabled"
